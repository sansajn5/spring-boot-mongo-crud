####################################################
# Configurazioni generali
####################################################

##Nome applicativo da utilizzare nel log
spring.application.name=${rootArtifactId}
spring.cloud.config.uri=http://${configMsHost:127.0.0.1}:${configMsPort:7891}/${configMsPath:configuration}
spring.profiles.active=${profile:svil}

spring.cloud.config.username=${configUser:test}
spring.cloud.config.password=${configPwd:test}